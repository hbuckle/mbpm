<UserControl x:Class="MailboxPermissionsManager.Views.AdvancedOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <StackPanel Background="AliceBlue" Margin="0">
        <TextBlock Text="Advanced Options" FontSize="14" TextDecorations="Underline" FontWeight="Bold"/>
        <TextBox Text="{Binding UserToRemove}"/>
        <Button HorizontalAlignment="Right" Content="Remove from all folders"/>
        <TextBlock Text="Full Access"/>
        <StackPanel Margin="0">
            <ComboBox/>
            <Button Content="Add" HorizontalAlignment="Right"/>
        </StackPanel>
        <Border Background="DarkGray" Height="4"/>
        <TextBlock Text="Primary Smtp"/>
        <StackPanel Margin="0">
            <TextBox/>
            <Button Content="Change" HorizontalAlignment="Right"/>
        </StackPanel>
        <Border Background="DarkGray" Height="4"/>
        <StackPanel Orientation="Horizontal" Margin="0">
            <TextBlock Text="Hidden from address lists"/>
            <CheckBox VerticalAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</UserControl>
